#include <stdio.h>

/**
 * main - writing the alphabet in lowercase
 *
 * Return: 0
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <='z'; letter++)
		puthar(letter);

	putchar('\n');

	return (0);
}
